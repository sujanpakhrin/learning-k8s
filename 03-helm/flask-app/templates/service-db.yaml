apiVersion: v1
kind: Service
metadata:
  name: flask-db
spec:
  selector:
    app: flask-db
  ports:
    - port: {{ .Values.mysql.service.port }}
      targetPort: {{ .Values.mysql.service.targetPort }}